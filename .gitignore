node_modules
dist
.env
.env.*
!.env.example
.expo
coverage
*.log
/android/
apps/backend/uploads/gpx/
.claude/worktrees/

# Runtime uploads (persisted via Docker volume in prod)
apps/backend/uploads/

# Zello handoff doc — contains live account passwords and private keys.
zello-integration.md
*.pem

# TS build info
*.tsbuildinfo
.DS_Store

# Local signing credentials — never commit (keystore passwords)
credentials.json
*.jks
*.keystore
!apps/mobile/android/app/debug.keystore
